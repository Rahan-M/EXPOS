int main()
{
decl
    int temp, num, pid;
enddecl
begin
    pid=exposcall("Fork");
    if(pid==0) then
        temp=exposcall("Exec", "evenNos.xsm");
    else 
        temp=exposcall("Wait", pid);
    endif;
    num=1;
    breakpoint;
    while ( num <= 100 ) do
        if((num % 2) == 1 ) then
            temp = exposcall ( "Write" , -2, num );
        endif;
        num = num + 1;
    endwhile;
    return 0;
end
}